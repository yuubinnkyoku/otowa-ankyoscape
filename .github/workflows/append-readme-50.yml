name: Append README research update 50

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/append-readme-50.yml'

permissions:
  contents: write

jobs:
  append:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Append verified research and clean temporary files
        shell: bash
        run: |
          set -euo pipefail
          if ! grep -Fq '# 64. 追加調査（2026-08-26・第50回）' README.md; then
            cat .tmp/research_append_20260826_50.md >> README.md
          fi
          rm -f .tmp/research_append_20260826_50.md .github/workflows/append-readme-50.yml
          rmdir .tmp 2>/dev/null || true
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          if git diff --cached --quiet; then
            exit 0
          fi
          git commit -m 'Document continued operation of Yanagibashi sewer trunk'
          git push
