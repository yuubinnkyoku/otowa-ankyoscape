name: append-readme-80
on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/append-readme-80.yml'
      - '.append-readme-80.txt'
permissions:
  contents: write
jobs:
  append:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Append research update and clean up
        shell: bash
        run: |
          set -euo pipefail
          cat .append-readme-80.txt >> README.md
          git rm .append-readme-80.txt .github/workflows/append-readme-80.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add README.md
          git commit -m 'Add research update 80: Nekoma Bridge chronology'
          git push
