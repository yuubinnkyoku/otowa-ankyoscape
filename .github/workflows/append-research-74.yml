name: append-research-74

on:
  push:
    paths:
      - '.github/workflows/append-research-74.yml'

permissions:
  contents: write

jobs:
  append:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Append research findings
        shell: bash
        run: |
          cat .append-research-74.md >> README.md
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add README.md
          git rm .append-research-74.md .github/workflows/append-research-74.yml
          git commit -m "Add cadastral findings around Shiina Station"
          git push
